<!DOCTYPE html>
<html lang="fr">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="{{ csrfToken }}" />
    <title>
      Mot de passe oublié - Twitter Clone
    </title>
    <script src="https://cdn.tailwindcss.com">

    </script>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
    />
  </head>

  <body class="bg-black text-white min-h-screen flex items-center justify-center">
    <div class="max-w-md w-full mx-4">
      <div class="bg-gray-900 rounded-xl p-8">
        <!-- Header -->
        <div class="text-center mb-6">
          <div class="w-16 h-16 bg-red-500 rounded-full flex items-center justify-center mx-auto mb-4">
            <i class="fas fa-key text-2xl text-white"></i>
          </div>
          <h1 class="text-2xl font-bold text-white">
            Mot de passe oublié
          </h1>
          <p class="text-gray-400 mt-2">
            Entrez votre adresse email et nous vous enverrons un lien pour réinitialiser votre mot de passe.
          </p>
        </div>

        @if(flashMessages.has('success'))
          <div class="mb-4 p-4 bg-green-600/20 border border-green-600/30 rounded-lg">
            <p class="text-green-400">
              {{ flashMessages.get('success') }}
            </p>
          </div>
        @endif
        
        @if(flashMessages.has('error'))
          <div class="mb-4 p-4 bg-red-600/20 border border-red-600/30 rounded-lg">
            <p class="text-red-400">
              {{ flashMessages.get('error') }}
            </p>
          </div>
        @endif
        
        <!-- Formulaire -->
        <form action="/password/forgot" method="POST" class="space-y-4">
          {{ csrfField() }}
          <div>
            <label for="email" class="block text-sm font-medium text-gray-300 mb-2">
              <i class="fas fa-envelope mr-2"></i>
              Adresse email
                    </label>
            <input
              type="email"
              id="email"
              name="email"
              required
              class="w-full px-4 py-3 bg-gray-800 border border-gray-600 rounded-lg text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
              placeholder="votre@email.com"
              value="{{ old('email') }}"
            />
          </div>

          <button
            type="submit"
            class="w-full bg-red-500 hover:bg-red-600 text-white font-bold py-3 px-4 rounded-full transition-colors"
          >
            <i class="fas fa-paper-plane mr-2"></i>
            Envoyer le lien de réinitialisation
                </button>
        </form>

        <!-- Navigation -->
        <div class="mt-6 text-center">
          <a href="/login" class="text-blue-400 hover:text-blue-300 underline">
            <i class="fas fa-arrow-left mr-2"></i>
            Retour à la connexion
                </a>
        </div>

        <!-- Informations de sécurité -->
        <div class="mt-8 p-4 bg-gray-800 rounded-lg">
          <h3 class="text-lg font-semibold mb-2 text-white">
            <i class="fas fa-shield-alt text-green-400 mr-2"></i>
            Sécurité
          </h3>
          <ul class="text-sm text-gray-400 space-y-1">
            <li>
              • Le lien de réinitialisation expire dans 1 heure
            </li>
            <li>
              • Vous ne pouvez l'utiliser qu'une seule fois
            </li>
            <li>
              • Si vous n'avez pas demandé cette réinitialisation, ignorez cet email
            </li>
            <li>
              • Vérifiez votre dossier spam si vous ne recevez pas l'email
            </li>
          </ul>
        </div>
      </div>

      <!-- Footer -->
      <div class="text-center mt-6">
        <p class="text-gray-500 text-sm">
          Besoin d'aide ?
                <a href="mailto:support@twitter-clone.com" class="text-blue-400 hover:text-blue-300 underline">
            Contactez le support
                </a>
        </p>
      </div>
    </div>
  </body>

</html>
